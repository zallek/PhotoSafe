### This file was generated by Nexus Schema
### Do not make changes to this file directly


"""
A date string, such as 2007-12-03, compliant with the `full-date` format
outlined in section 5.6 of the RFC 3339 profile of the ISO 8601 standard for
representation of dates and times using the Gregorian calendar.
"""
scalar Date

type Face {
  h: Int!
  id: Int!
  identity: Identity
  photo: Photo!
  w: Int!
  x: Int!
  y: Int!
}

type Identity {
  faces: [Face!]!
  id: Int!
  name: String!
}

type Mutation {
  createFace(h: Int!, photoId: Int!, w: Int!, x: Int!, y: Int!): Face!
  createIdentity(faceId: Int, name: String!): Identity!
  deleteFace(faceId: Int!): Face!
  deleteIdentity(identityId: Int!): Identity!
  identifyFace(faceId: Int!, identityId: Int!): Face!
  scanPhotos: Boolean!
}

type Photo {
  faces: [Face!]!
  id: Int!
  path: String!
}

type Query {
  identities: [Identity!]!
  identity(identityId: Int!): Identity!
  photo(photoId: Int!): Photo!
  photos(identityId: Int): [Photo!]!
}
